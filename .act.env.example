# act environment — copy to .act.env (make act-init).
# Values here land in the job environment for every step.

PYTHONUNBUFFERED=1

# No UV_FROZEN: it conflicts with `uv sync --locked`, which CI uses.

# Keep uv's cache inside the workspace so --bind reuses it between runs.
UV_CACHE_DIR=/workspace/.uv-cache
